spring.datasource.url=jdbc:mysql://localhost:3306/employee?useSSL=false
spring.datasource.username=admin
spring.datasource.password=admin
#spring.datasource.driver-class-name=com.mysql.jdbc.Driver
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5Dialect

# Create the schema and tables if they don't exist, but do not modify them if they do.
#spring.jpa.hibernate.ddl-auto=update

# Create the schema and tables from scratch every time the application starts.
spring.jpa.hibernate.ddl-auto=create

